# Design Notes

The design notes that lived here are now part of the docs site, rewritten as prose:

- [`docs/about/architecture.md`](docs/about/architecture.md) — why the parser is this
  package's own, the wrapper contract, the engine lifecycle, the styles and fonts policy,
  and the client design.
- [`docs/reference/options.md`](docs/reference/options.md) and
  [`docs/reference/api.md`](docs/reference/api.md) — the option and API surface.

Run the site locally with `pnpm docs:dev`.
