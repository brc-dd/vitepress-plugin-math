dist
coverage
pnpm-lock.yaml

# markdown is excluded wholesale: test/fixtures/*.md are exact parser
# input/output pairs (the `name . input . expected .` format is
# whitespace-exact), and the example docs are math-heavy content where
# prettier's markdown escaping can change TeX semantics
*.md
